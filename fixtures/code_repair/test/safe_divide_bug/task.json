{
  "id": "test-safe-divide-bug",
  "description": "Repair safe_divide so it coerces numeric strings, returns None for zero denominator or invalid numeric input, preserves the float quotient, and rejects booleans by returning None.",
  "entrypoint": "division.py"
}
