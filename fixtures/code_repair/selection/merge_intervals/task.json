{
  "id": "selection-merge-intervals",
  "description": "Repair merge_intervals so it returns a new deterministic sorted list of inclusive intervals. Normalize reversed endpoints before processing, merge intervals that overlap or touch, reject malformed intervals with ValueError, and do not mutate the input.",
  "entrypoint": "intervals.py"
}
