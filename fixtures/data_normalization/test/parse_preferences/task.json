{
  "id": "data-test-parse-preferences",
  "description": "Repair preference_flags so it returns normalized preference booleans. Read preference names from name, preference, key, or setting aliases; trim and lowercase names, and skip blank or missing names. Values 'on', 'enabled', 'yes', '1', 1, and True mean True; all other values, missing values, blank strings, and None mean False. Later rows override earlier rows for the same normalized preference name.",
  "entrypoint": "preferences.py"
}
